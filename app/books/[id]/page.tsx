import Link from "next/link";
import { ArrowLeft, BookOpen, CheckCircle2 } from "lucide-react";
import Sidebar from "@/components/sidebar";
import MobileNavigation from "@/components/mobile-navigation";
import { getBooks } from "@/lib/api/books";

export default async function BookDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const books=await getBooks();const book=books.find(item=>item.id===id)||books[0];return <div className="shell"><Sidebar/><MobileNavigation/><main className="page-main"><div className="container"><Link href="/books" className="btn" style={{padding:0,marginBottom:22,color:"#071a33"}}><ArrowLeft size={18}/> Retour à la bibliothèque</Link><section className="card" style={{display:"grid",gridTemplateColumns:"minmax(220px,300px) 1fr",gap:30,alignItems:"center"}}><div className="cover" style={{height:390,borderRadius:18}}>{book.cover_url&&<img src={book.cover_url} alt={book.title}/>}</div><div><p className="eyebrow" style={{color:"#9a6b0e"}}>Livre numérique</p><h1 style={{fontSize:"clamp(30px,5vw,48px)",color:"#071a33",margin:"8px 0 12px"}}>{book.title}</h1><p className="muted" style={{fontSize:16}}>{book.author||"Auteur non renseigné"}</p><p style={{lineHeight:1.8,color:"#475569"}}>{book.description||"Cette ressource est disponible dans Bickri Lib."}</p><div className="feature-list" style={{margin:"20px 0"}}><div className="feature"><div className="feature-icon"><CheckCircle2 size={18}/></div><span>Lecture numérique sécurisée</span></div><div className="feature"><div className="feature-icon"><CheckCircle2 size={18}/></div><span>Accessible depuis ordinateur et mobile</span></div></div><button className="btn btn-gold"><BookOpen size={18}/>Commencer la lecture</button></div></section></div></main></div>}
